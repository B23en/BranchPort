# 압축 성능 평가 지표

> 2026-08-05 작성 · **2026-08-07 갱신**: §2에 QA 보존율 파일럿 실측 추가,
> §5 평가용 데이터셋 조사(코딩 세션·QA 벤치마크·일반 대화·한국어) 신설, §4.1 우선순위 표에 QA 보존율 반영.
> **2026-08-09 갱신**: §2에 목적 프로파일 A/B 실측(v3.7→v3.8.1) 추가.

---

## 결론 요약

1. **압축 성능의 단일 표준 지표는 존재하지 않는다.** 학계·업계의 사실상 표준은 **2축 동시 보고**: ①압축률(배수/%) + ②품질 보존율(%). "N배 압축에 원래 성능 M% 유지" 형태가 지배적. ([Prompt Compression in the Wild](https://arxiv.org/html/2604.02985)는 압축률/다운스트림 성능/grounding/정보 보존 4축을 명시.) 여기에 **③효율성(지연시간·메모리·비용)** 축을 함께 보고하는 관행이 최근 추세 — 상세는 §3.1.
2. BranchPort의 압축은 두 종류이고 **평가 지표를 분리해야 한다**:
   - **구조적 압축**(레코드 ~1,000개 → 턴 10\~80개, 손실 없는 접기) → 그래프 요약·코드 폴딩 분야 지표
   - **내용 압축**(LLM 요약 생성) → 요약 품질·사실성 지표
3. 정답(참조) 요약이 없는 우리 상황에서 로컬 실행 가능한 최적 후보는 **SummaC**(NLI 기반 사실적 일관성, `pip install summac`, CPU 가능).
4. **Claude Code `/compact` 자체를 정량 평가한 공식 벤치마크는 없다** — 커뮤니티 정성 보고 수준. 우리가 압축 품질을 정량 측정하면 그 자체가 차별점.
5. 문제 설정이 거의 동일한 선행연구 2건을 확보: **Autofolding/TASSAL**(코드 폴딩=손실 없는 접기), **Githru**(git DAG 집약 시각화 + 사용자 연구). 결과보고서 related work에 직접 인용 권장.
6. CLI 코딩 에이전트의 컨텍스트 압축을 직접 다룬 검증된 선행연구를 추가 확보: **OPENDEV ACC**(5단계 점진 압축, 관측값 peak 54% 절감, "아카이브로 사실상 무손실" — 우리와 동일 철학), **SelfCompact**(모델이 압축 시점 자체 판단, 비용 30\~70% 절감 + 정확도 상승), **OpenHands condenser 실측**(SWE-bench 성공률 유지 + 비용 절반), **Control Under Compression**(예산 75%까지 안전, 50\~35%에서 급락하는 신뢰도 경계). 최근 보고 관행은 단일 수치 대신 **트레이드오프 곡선/frontier**(§3.5). 상세는 §3.3\~3.5.

---

## 1. 구조적 압축(접기)의 지표 — 그래프 요약·코드 폴딩·HCI

| 지표                         | 정의/측정                                                                   | BranchPort 적용                                                                                                   | 출처                                                                                           |
| ---------------------------- | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| **압축률**             | `1 − (턴 수 / 원시 레코드 수)` 또는 배수                                 | 즉시 계산 가능. 실측 기준 중앙값 161→10, 최대 1,007→80 (≈94% 감소)                                             | [Graph Summarization Survey](https://gemslab.github.io/papers/liu-2018-graph-summarization.pdf) |
| **재구성 완전성**      | 요약본에서 원본 복원 시 오차(reconstruction error)                          | 접기는 무손실이므로 "재구성 오차 0 — 턴을 펼치면 원본 레코드 100% 복원" 문구로 사용                              | [CGS](https://arxiv.org/html/2607.10969v1)                                                      |
| **질의 보존**          | 요약본에서 원본과 동일 질의에 답 가능한지                                   | "턴 뷰만으로 '어떤 파일이 수정됐나' 질의에 100% 답변" 자동 검증 가능                                              | [CGS](https://dl.acm.org/doi/10.1145/3786788)                                                   |
| **화면/스크롤 감소율** | 화면당 표시 항목 수, 스크롤 길이 비교                                       | 원시 JSONL 뷰 vs 턴 뷰의 스크롤 횟수 비교 → "스크롤 N% 감소"                                                     | [Event Sequence 시각화 서베이](https://arxiv.org/pdf/2006.14291)                                |
| **과제 완료 시간 A/B** | 동일 질의("실패한 툴콜 찾기")를 원시 로그 vs 도구로 수행, 시간·정답률 비교 | Githru가 개발자 12명 통제 실험으로 이 방법 사용. 팀 내부 소규모로 재현 가능 —**심사위원 설득력 최고 후보** | [Githru (IEEE VIS 2020)](https://arxiv.org/abs/2009.03115)                                      |
| PREVis 가독성 설문           | 4차원 11문항 검증된 설문 척도                                               | 압축 전/후 뷰에 팀원 설문 → 정량 가독성 점수                                                                     | [PREVis](https://arxiv.org/abs/2407.14908)                                                      |

**선행연구 직접 인용 포인트**:

- **Autofolding/TASSAL** — "폴딩 후 길이가 원본의 특정 비율 이하 + 안 접힌 부분이 가장 중요한 내용"으로 압축 문제를 정의. "코드 폴딩의 아이디어를 대화 로그에 적용했다"는 서사에 사용. ([논문](https://arxiv.org/abs/1403.4503))
- **Githru** — git 커밋 DAG를 집약(Context-Preserving Squash Merge)해 시각화 + 과제 완료 시간으로 평가. 문제 설정·평가 방법 모두 우리와 동일 구조. ([논문](https://arxiv.org/abs/2009.03115))

## 2. 내용 압축(LLM 요약)의 지표 — 요약 품질·사실성

### 참조(정답 요약) 없이 쓸 수 있는 것 — 우리 상황의 주력

| 지표                  | 방식                                             | 로컬 실행                             | 권고                                                                                                           |
| --------------------- | ------------------------------------------------ | ------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **SummaC**      | NLI로 원문×요약 문장 함의 행렬 → 사실적 일관성 | ✅`pip install summac`, CPU 가능    | **1순위.** "요약이 원문 로그에서 함의되는 내용만 담는가"                                                 |
| QAFactEval            | QA 기반 사실성 (성능은 최고 수준)                | ⚠️ torch 1.6 종속, 최신 환경 비호환 | 비추천 (유지보수 부담)                                                                                         |
| QuestEval             | QG+QA 파이프라인                                 | ⚠️ 모델 2개, 무거움                 | 보류                                                                                                           |
| G-Eval (LLM-as-judge) | LLM에 CoT 채점 지시                              | △ 요약 생성에 쓰는 LLM 재활용        | 보조. 신뢰성 논쟁 있음 — 쓴다면**세밀한 점수 대신 이진 판정** ([근거](https://arxiv.org/pdf/2603.05399)) |

### 대화 로그 특화 지표 (일반 요약과 다른 부분)

- **화자/주체 귀속 오류율**: 대화 요약 모델 출력의 33\~42%가 "누가 무엇을 했는지" 혼동 오류 포함 ([연구](https://arxiv.org/pdf/2305.16548)). 우리 버전: **툴콜 주체·대상 파일 오귀속 비율** — 세션 로그 요약에서 특히 중요.
- **Key-fact recall**: 수정된 파일명·실행 명령 같은 핵심 사실이 요약에 남아있는 비율 ([정보 보존 논문](https://arxiv.org/html/2503.19114)). 우리 데이터는 툴콜 레코드에서 정답을 기계적으로 추출할 수 있어 **자동 채점 가능** — ROUGE보다 훨씬 우리에게 유리한 지표.
- ROUGE류는 뉴스 기사용 설계라 대화 요약에 부적합하다는 것이 정설(SAMSum 저자 지적) — **ROUGE 단독 주장은 학계 기준 약한 근거**. 골드셋(사람이 쓴 요약)을 소량 만들면 ROUGE-L+BERTScore 보조 사용 가능.
- 평가 차원 프레임: **SummEval 4차원**(coherence/consistency/fluency/relevance)을 채점 체크리스트로 인용 ([SummEval](https://github.com/Yale-LILY/SummEval), [DialSummEval](https://aclanthology.org/2022.naacl-main.418/)).

### QA 보존율 (2026-08-07 파일럿 실측)

key-fact recall이 *추출형* 보존(파일명·명령이 그대로 남았나)을 재는 것과 달리, **원문 트랜스크립트에서
사실 질문을 생성해 "압축 패키지만 보고" 답하게 하는 QA 방식**은 *이해형* 보존(결정 근거·에러 경위·작업
상태 같은 서술형 사실)을 잰다. [QAGS](https://aclanthology.org/2020.acl-main.450/)류 QA 기반 사실성 평가의
표준 절차를 압축 평가에 적용한 것. 명칭은 **"QA 보존율"**로 통일한다 (질문은 원문 트랜스크립트에서
자체 생성 — 기성 QA 데이터셋 사용 아님, §5.2 참고).

**파이프라인** (4단계, 전부 `claude -p` 이진 판정 — §2 G-Eval 권고와 일치):

1. **질문 생성**: 트랜스크립트에서 사실 질문 10개 + 정답. 유형 분산(파일·명령 / 에러·해결 / 결정·근거 /
   수치 / 작업 상태 — [LoCoMo](https://snap-research.github.io/locomo/)의 질문 유형 분류 차용)
2. **패키지 답변**: 압축 패키지 md만 근거로 답변, 없으면 "모름" (abstention — LongMemEval 차용)
3. **무맥락 대조(closed-book)**: 자료 없이 같은 질문에 답변 — 일반 지식으로 찍어 맞출 수 있는 질문 보정
4. **이진 채점**: 의미 동등이면 정답. **보존율 = 패키지 정답 / 전체**, 무맥락 정답률 병기

**파일럿 결과** (2026-08-07, 한국어 로컬 세션 6턴 구간, 트랜스크립트 33,257자 → 패키지 7,217자 = 잔존율 21.7%, v3.4·기본 모델):

| 측정값 | 결과 |
|---|---|
| QA 보존율 | **9/10 (90%)** |
| 무맥락 대조 | 1/10 |
| 비용 (sonnet 4회) | $0.363 |

- 유일한 오답: 원문 속 연구 수치(PCC F1 63.62/66.29)가 패키지에서 탈락 — **구체적 수치가 출력 예산
  압력에 밀리는 잔존 결함**의 실증. v3.4 "식별자>예산" 규칙의 개선 소재.
- 무맥락 정답 1건(`pip install pypdf`)은 일반 지식으로 추측 가능한 질문 — **질문 생성 단계에
  "무맥락으로 답 가능한 질문 배제" 필터 필요**하다는 설계 교훈.
- 산출: "잔존율 21.7%로 압축해도 사실 질문의 90%에 답할 수 있다" — §4.2의 "절감률·보존율 동시 보고"
  세트를 채우는 두 번째 품질 축.

**배치 실측** (2026-08-07, SWE-chat 한국어 트랙 13세션 × 10문항 = 130문항, sonnet·cut 0.8,
스크립트 `오픈소스/probe-test/qa-preservation.mjs` — 질문 생성에 "일반 지식으로 답 가능한 질문 금지"
필터 반영):

| 측정값 | 결과 |
|---|---|
| **QA 보존율 (마이크로 평균)** | **112/130 (86%)** · 세션별 60\~100% |
| 무맥락 대조 | **0/130 (0%)** — 질문 필터가 찍기 가능성을 제거 |
| compact 성공률 | 13/13 (실패 0) · 비용 합 $3.17 |
| QA 채점 비용 | $4.26 (세션당 $0.33) |

핵심 발견:

- **오답 18건 전원이 "모름" — 틀린 사실을 답한 경우 0건.** 압축의 손실 모드가 "왜곡·환각"이 아니라
  "정직한 누락"이라는 실측 근거. 패키지가 모르는 것을 아는 척하지 않으므로, 받은 쪽은 /expand로
  원문을 조회하면 된다(무손실 계층 설계와 부합).
- **탈락한 사실은 압도적으로 구체적 수치** — 줄 수(519줄·5,481줄), 글자 수 제한(200자), 포트 번호(:8001),
  필드 값(priority: high) 등. 파일럿의 PCC F1 수치 탈락과 동일 패턴의 규모 재현 —
  v3.4 "식별자>예산" 규칙이 파일명·명령은 지키지만 **본문 속 수치까지는 못 지킨다**는 정량 증거.
- **잔존율↔보존율 트레이드오프 실측점 확보**: 잔존율 13%로 가장 세게 압축된 세션(kr-2b3756eb)이
  보존율 60%로 최저 — §4.1 우선순위 5(트레이드오프 곡선)의 곡선 위 점들이 생겼다.
- 원자료: `오픈소스/probe-test/batch-results/swechat-korean/`(runs.jsonl · qa-runs.jsonl · qa-summary.md).

**v3.5 수치 보존 규칙 회귀 측정** (2026-08-07, 위 결과를 근거로 프롬프트에 numeric preservation
규칙 추가 — 본문 수치를 식별자와 동급으로 승격. 동일 13세션·**동일 질문 130문항**·동일 무맥락
답변 재사용으로 통제 비교):

| | v3.4 | v3.5 |
|---|---|---|
| QA 보존율 | 112/130 (86%) | **120/130 (92%)** |
| 잔존율(가중) | 30% | 31% — **크기 회귀 없음** |
| compact 성공률 | 13/13 | 13/13 |

- 세션별 6개 ▲상승(최대 +3문항), 6개 =동일, 1개 ▼하락(-1문항) — 상승이 지배적.
- 남은 오답 10건도 8건이 더 깊은 꼬리 수치(중간 집계값·커밋 해시 등)이고 여전히 전부
  "모름"(환각 0) — 손실 모드 서사("정직한 누락")는 v3.5에서도 유지된다.
- 원자료: `오픈소스/probe-test/batch-results/swechat-korean-v35/`.

**질문 유형별 집계** (130문항을 5유형으로 LLM 분류 — 정답이 수치면 numeric 우선.
라벨: `swechat-korean-v35/cache/type-labels.json`):

| 유형 | 문항 | v3.4 | v3.5 |
|---|---|---|---|
| numeric (수치·측정값) | 44 | 31/44 (70%) | **38/44 (86%)** |
| error_fix (에러·해결) | 28 | 28/28 (100%) | 27/28 (96%) |
| file_cmd (파일·명령) | 25 | 25/25 (100%) | 24/25 (96%) |
| decision (결정·근거) | 24 | 20/24 (83%) | 22/24 (92%) |
| status (작업 상태) | 9 | 8/9 (89%) | 9/9 (100%) |

- v3.4에서 이미 error_fix·file_cmd가 100% — 기존 "식별자·에러 verbatim" 규칙이 작동한다는 교차 확인.
- v3.5의 개선은 **목표 유형(numeric)에 +16%p로 집중**되고 나머지 유형은 96\~100% 유지 —
  규칙 추가가 의도한 곳만 움직였다는 회귀 증거.

**모델별 비교 — 고정 벤치마크** (2026-08-07): 질문지·무맥락 답변·답변자·채점자(sonnet)·압축
프롬프트(v3.5)·cut(0.8)을 전부 고정하고 **압축 모델만 변수**로 둔 비교. 데이터는 두 트랙 —
SWE-chat 한국어 13세션(130문항)과 **trace-commons 층화 12세션**(5.7k\~120k자, 120문항.
CC-BY라 인용·재배포 가능해 벤치마크 본체로 선정. Fable-5는 중앙값 6.6k자·36/76이 손익분기
미만 + AGPL이라 제외):

| 압축 모델 | SWE-chat(한국어) | trace-commons(영어) | 잔존율 | 압축 비용(12\~13세션) |
|---|---|---|---|---|
| sonnet | **120/130 (92%)** | 103/120 (86%) | 31% / 36% | $3.2 / $3.2 |
| opus | 119/130 (92%) | **109/120 (91%)** | 30% / 33% | $5.2 / $4.3 |
| haiku | 99/130 (76%) | 97/120 (81%) | 31% / 31% | $1.0 / $1.0 |

- **sonnet ≈ opus**: 한국어 트랙 동률(92%), 영어 중대형에서 opus +5%p. 비용은 opus가 1.3\~1.6배 —
  실험 문서 §4.2의 "품질 기본값 opus, 비용 민감 시 sonnet" 권고와 정합.
- **haiku는 뚜렷한 품질 하락**(76\~81%) — 비용 1/3이지만 보존율 10%p 이상을 지불한다.
  §3.5 비용-품질 frontier의 실측 3점.
- opus에서 JSON 절단 1건 발생, 재실행으로 통과(10/10) — 자동 재시도 설계의 필요성 재확인.
- 원자료: `오픈소스/probe-test/batch-results/{swechat-korean-*,tcbench-*}/`.

### Key-fact recall (2026-08-08 실측 — 우선순위 4)

QA 보존율이 *이해형* 보존(서술형 사실)을 재는 것과 달리, key-fact recall은 *추출형* 보존을 잰다 —
**툴콜 레코드에서 기계 추출한 식별자(수정 파일명·실행 명령)가 요약 텍스트에 그대로 남았는가.**
LLM 호출이 전혀 없는 결정론적 문자열 채점이라 **비용 0**이고, 기존 배치 결과 전체에 소급 적용했다.
스크립트: `오픈소스/probe-test/keyfact-recall.mjs` (로컬 전용, qa-preservation.mjs와 동일 관례).

**설계** — 채점 대상을 둘로 나눈 것이 핵심:

- 정답지: 구간 턴의 툴콜에서 기계 추출 — 수정 파일(Edit/Write/NotebookEdit의 basename),
  실행 명령(Bash preview, 시크릿 마스킹 건은 제외). 파일은 basename 포함 여부,
  명령은 **exact**(preview 전문)와 **head**(앞 2토큰 = 프로그램+서브커맨드) 2단계로 판정.
- **llm** = 패키지의 LLM 산출층(summary JSON)만 채점 — 요약 프롬프트 규칙만으로 지켜지는 몫.
  내장 `/compact`처럼 요약층만 있는 도구가 갖는 구조와 같다.
- **full** = 사실층이 병합된 최종 md 패키지 채점 — 구조적 병합의 보증을 검증.
- 정답 추출과 텍스트 채점이 분리돼 있어, 나중에 내장 `/compact` 산출물(자유 텍스트)에도
  같은 정답지로 채점 가능(§4.2 비교의 사전 준비).

**결과** (마이크로 평균, cut 0.8, 7개 배치 소급 채점):

| 트랙 (압축 모델) | 수정 파일 llm | 수정 파일 **full** | 명령 head llm | 명령 head full |
|---|---|---|---|---|
| SWE-chat v3.4 (sonnet) | 132/169 (78%) | **169/169 (100%)** | 30% | 80% |
| SWE-chat v3.5 (sonnet) | 117/169 (69%) | **169/169 (100%)** | 47% | 81% |
| SWE-chat v3.5 (opus) | 61% | **100%** | 42% | 81% |
| SWE-chat v3.5 (haiku) | 43% | **100%** | 8% | 80% |
| trace-commons v3.5 (sonnet) | 124/228 (54%) | **228/228 (100%)** | 15% | 65% |
| trace-commons v3.5 (opus) | 45% | **100%** | 25% | 72% |
| trace-commons v3.5 (haiku) | 32% | **100%** | 4% | 65% |

핵심 발견:

- **수정 파일 recall: full은 전 트랙·전 모델 100% (397/397)** — 사실층 기계 병합이 주는 구조적
  보증의 실측 확인. 반면 LLM 산출층만으로는 32\~78%에 그친다: 요약이 "컴포넌트 14개 생성"처럼
  열거를 개괄로 눌러버리는 순간 파일명이 소실된다(kr-08993363에서 .jsx 14개 탈락이 대표 사례).
  **요약층에만 의존하는 도구(내장 `/compact` 구조)는 수정 파일 식별자의 22\~68%를 잃는다는 뜻** —
  주장 B의 차별점 서사("LLM이 못 지키는 것을 구조가 지킨다")를 정량으로 뒷받침.
- **명령 full이 100%가 아닌 이유는 사실층의 20개 캡**(당시 server.ts `commands.slice(0, 20)`) — 명령이
  많은 세션(80\~157개)에서 초과분이 탈락한다. 캡 상향/전량 포함이 즉효 개선 후보(패키지 크기와
  트레이드오프 — 명령 preview는 90자 캡이라 전량도 수 KB 수준).
  → **캡 60으로 상향 완료(2026-08-08)**: full 명령 recall 97%로 개선 — 실증은 아래 컷 곡선 §의 ⑧.
- **QA 보존율과의 역방향 실증**: v3.4→v3.5에서 QA 보존율은 86→92% 올랐지만 수정 파일 llm recall은
  78→69%로 내렸다(명령 head는 30→47% 상승). 출력 예산 안에서 보존 우선순위가 이동한 것 —
  **두 지표가 서로 다른 축을 잰다는 직접 증거**이고, 병기해야 하는 이유. llm층의 파일 손실은
  full 병합이 전부 메우므로 실사용 영향은 없다.
- 모델 축은 QA 보존율과 정합: llm층 recall은 sonnet > opus > haiku, haiku는 여기서도 최하.
- 명령 exact는 전 트랙 0\~3% — 요약은 명령 전문을 유지하지 않는다(당연한 압축 동작).
  실용 축은 head("무슨 명령을 쳤는지")이고, 전문이 필요하면 full의 사실층·/expand가 답.

한계(정직 보고): basename 부분일치라 다른 맥락의 동명 언급도 인정될 수 있고(위양성),
head 2토큰은 관대한 기준. 재구성 기반이라 로그가 변하면 정답지도 변한다(스크립트가
기록 턴 수와 불일치 시 경고). 이 지표는 "남았는가"만 재며 "맞게 남았는가"(오귀속)는 못 잰다 —
후자는 QA 보존율·감사가 커버.

### Claude Code 내장 `/compact` 대비 비교 (2026-08-08 실측 — §4.2의 "최초 실측")

`/compact`를 정량 평가한 공식 벤치마크가 없으므로(결론 4) 직접 쟀다. 핵심 설계:
**내장 요약을 재현하지 않았다** — SWE-chat·trace-commons 세션 11개에 실제 프로덕션 auto-compact가
남긴 `compact_boundary` + `isCompactSummary` 레코드가 들어 있어, **진짜 산출물 원문**을 그대로
비교 대상으로 썼다(재현 부정확성 반론 원천 차단). 경계 타임스탬프로 "내장이 요약한 구간"
(세션 시작→첫 경계)을 특정하고, 같은 구간으로 BranchPort compact(v3.5·sonnet)를 돌려
동일 질문지(QA 보존율)·동일 정답지(key-fact recall)로 맞대결했다.
스크립트: `오픈소스/probe-test/compact-vs-builtin.mjs`, 원자료: `batch-results/builtin-compare/`.

**결과** (11세션 = 한국어 4 + 영어 7, 질문 110개, 전 이벤트 trigger=auto·preTokens 167k\~172k):

| | BranchPort (full md) | 내장 /compact |
|---|---|---|
| QA 보존율 | **99/110 (90%)** | 79/110 (72%) |
| 수정 파일 recall | **240/240 (100%)** | 161/240 (67%) |
| 명령 head recall | **206/267 (77%)** | 55/267 (21%) |
| 크기(합) | 255,575자 | 161,362자 |
| 무맥락 대조 | 0/110 (양쪽 공통 질문지) | — |

핵심 발견:

- **QA 보존율 +18%p (90% vs 72%)** — 내장이 이긴 세션은 11개 중 1개(kr-1bee228b, +1문항),
  동률 2개, 나머지 8개는 우리가 우세(최대 +3문항).
- **구조 병합의 차별점이 그대로 재현**: 내장의 파일 recall 67%는 우리 *llm층 단독*(73%)과
  비슷하다 — 즉 LLM 요약의 한계는 양쪽 공통이고, 차이를 만드는 것은 사실층 기계 병합(100%)이다.
  대형 세션에서 특히 극적: tc-39082932(수정 파일 78개)에서 내장은 13/78(17%)로 붕괴, 우리는 78/78.
- **명령이 최약점(21%)**: 내장 요약은 "무슨 명령을 실행했는지"를 거의 남기지 않는다
  (`wc -l`·`rm`·`git config --global --add safe.directory` 등 세션 고유 명령 질문에 일관되게 "모름").
  QA 오답에서도 같은 패턴 — 명령·수치 질문에서 집중적으로 실패.
- **손실 모드 차이 — 내장은 작업 상태를 왜곡한 사례가 있다**: 내장 오답 23건 중 "모름"이 아닌
  오답 6건, 그중 **완료↔미완료를 뒤집은 상태 왜곡 2건**(kr-2b3756eb: 미완료인 security.md
  마이그레이션을 "완료"로, tc-4222016d: 완료된 M2·M3 마일스톤을 "미완료"로). 우리 오답 11건은
  "모름" 8 + 경미한 부정확 3(예: "14개"를 "14개 이상"으로)이고 상태 왜곡은 0 —
  v3의 time-stamping·open_threads 규칙이 지키는 지점이 실측에서 갈렸다.
- 전 이벤트가 auto trigger·preTokens 167k대 — **자동 compact는 발동선(window−13k) 직전에서
  일어난다**는 실측 부산물. 압축 시점 시각화(우리 도구의 측정 대상)와 연결되는 수치.

**공정성 한계** (보고서에 반드시 병기):

- 내장 요약의 생성 모델·버전이 세션마다 다르고 미상(v2.1.39\~2.1.168 시기의 프로덕션 설정) —
  **제품 수준 비교**이지 같은 모델로 통제한 프롬프트 수준 비교가 아니다.
- 크기가 우리 쪽이 평균 1.6배 크다(사실층·diff·앵커 포함) — QA에 구조적으로 유리할 수 있다.
  반례로 우리가 더 작으면서 크게 이긴 세션 2개(kr-2b3756eb 9.8k vs 10.6k에서 8:5,
  kr-995c0a21 13.5k vs 14.0k에서 9:7)가 있어 크기만의 효과는 아니다.
- 질문지는 우리 렌더 트랜스크립트(120k자 캡)에서 생성 — 내장은 전체 컨텍스트(167k+ 토큰)를
  요약했으므로 요약 예산이 더 넓은 범위에 분산된다. 단 질문된 사실 자체는 양쪽 입력에 모두 존재.
- 내장 요약의 목적 함수는 "같은 세션의 대화 재개"로 우리(타 세션 이관 패키지)와 다르다.

### 컷(구간 크기)별 트레이드오프 곡선 (2026-08-08 실측 — 우선순위 5)

같은 세션에서 구간 크기(컷 0.3/0.5/0.8/1.0)를 바꿔가며 compact를 돌려 잔존율과 LLM층
key-fact recall을 결정론 채점했다(한국어 트랙 대형 4세션 × 4컷 = 16점, sonnet, 비용 $6.2).
스크립트 `오픈소스/probe-test/cut-curve.mjs`, 원자료 `batch-results/cut-curve/`.

| 세션 | 컷 0.3 → 1.0 흐름 | 잔존율 | llm 파일 recall |
|---|---|---|---|
| kr-01abe31e | 파일 2→31개 | 39→50% | 100% 유지 → **@1.0에서 74%로 하락**(31개) |
| kr-1bee228b | 파일 7→14개 | 32→26% | 전 구간 100% |
| kr-2b3756eb | 파일 6→16개 | 17→11% | 전 구간 100% |
| kr-995c0a21 | 파일 22→53개 | 39→31% | **전 구간 25\~45%**(항상 대량) |

핵심 발견:

- **LLM층 식별자 보존의 임계 변수는 구간 길이가 아니라 "열거 크기"다.** 수정 파일 \~20개까지는
  컷과 무관하게 100%를 유지하고, 30개를 넘으면 컷과 무관하게 무너진다(파일 22\~53개인
  kr-995c0a21은 어느 컷에서도 25\~45%). 출력 예산 안에서 열거가 개괄로 눌리는 임계가 존재 —
  **"구조(사실층)가 왜 필요한가"의 곡선 버전 증거**이고, full recall은 이 임계와 무관하게 100%.
- **잔존율은 구간이 클수록 개선**(더 세게 압축): 17→11%, 32→26% 등 — 출력 예산 상한이
  큰 구간에서 압축 압력으로 작동한다. 반대로 작은 구간은 골격 비용 탓에 잔존율이 높다
  (§ "손익분기" 서사와 정합).
- **명령 캡 20→60 상향(⑧)의 실증**: 16점 합계 full 명령 recall **604/625 (97%)** — 캡 20
  시절 65\~81%에서 상승. 명령 ≤60인 13점은 전부 100%, 60 초과 3점(73\~84개)만 87\~96%로
  생략 표시("외 N개 생략")가 명시된다.
- 한계: 곡선 점이 세션당 1회 실행이라 요약 변동 노이즈가 있다(kr-01abe31e@0.8→@1.0의
  파일 상실 6건 중 일부는 재생성 변동 추정). 점당 반복 실행 평균이 보강 후보.

### 언어 보존 검증 (2026-08-08 실측 — 한국어 트랙 ⑦)

compact 프롬프트의 "세션 주 언어로 작성" 규칙이 지켜지는지 **결정론 측정**했다(LLM 호출 없음).
정답은 트랙이 아니라 세션 단위 — 사용자 프롬프트의 한글 비율 ≥15%면 한국어 세션. 스크립트
`오픈소스/probe-test/lang-preserve.mjs` (회귀 검증 도구로 유지).

**실측 (기존 패키지 75개, v3.5 시점)**:

| 모델 | 한국어 세션 유지 | 영어 세션 한글 누출 |
|---|---|---|
| sonnet | 통과 | 0/12 누출 (tc 트랙) — 단 재현 프로브에서는 누출 발생 |
| opus | 통과 | **5/12 누출 (한글 24\~30%)** |
| haiku | **5건 영어화 (한글 0\~1%)** | 1건 역방향 누출 |

핵심 발견:

- **누출된 영어 세션 5개의 원본에는 한글이 0자** — 오염이 세션 내용이 아니라 외부에서 왔다는
  결정적 증거. 재현 프로브(동일 영어 트랜스크립트 요약)에서 이번엔 opus가 영어(정상),
  **sonnet이 한국어(위반)** — 즉 특정 모델의 문제가 아니라 **운영자 언어 설정("Always respond
  in korean")이 `--system-prompt` 대체에도 불구하고 두 모델 모두에 비결정적으로 누출**된다.
  prompt.ts의 기존 가정("대체 시 CLAUDE.md 미로드 — 기본 모델에서 확인")은 항상 성립하지
  않는다(버전·경로 종속).
- **haiku는 반대 방향으로도 위반** — 한국어 세션 5건을 영어로 요약(fork-resume 영어화 문제의
  재현). 언어 축에서도 haiku 비추천 근거 추가.
- 부수 발견: trace-commons에도 한국어 사용자 세션이 섞여 있다(§5.1의 "전부 영어" 기술은
  세션 단위로는 부정확 — 세션별 판정 필요).

**대응 — 프롬프트 v3.6** (2026-08-08): 언어를 모델 판단에 맡기지 않는다.
① 서버가 사용자 프롬프트 한글 비율로 주 언어를 결정론 판정(≥15% ko / <5% en / 사이는 보류)해
**명시 지시**("Write every free-text value in Korean/English")로 주입 — 추상 규칙("주 언어로")은
혼합 세션에만 남는다. ② 산출물 언어가 판정과 어긋나면 1회 재시도(파싱 재시도와 같은 관례).
③ meta에 `lang`·`userHangulPct` 계측 추가.

**검증**: 이전 위반 3건(영어 세션×opus 2, 한국어 세션×haiku 1) 재실행 — **3/3 전부 1차
시도에서 올바른 언어**(재시도 불필요: 영어 세션 한글 0%, 한국어 세션 한글 57%).

한계: 스팟 검증 3건(전 트랙 재생성은 비용상 보류 — lang-preserve.mjs로 회귀 감시), 한글
비율 지표는 한국어 전용(타 언어 미커버), 혼합 세션(한글 5\~15%)은 판정 보류로 기존 규칙
유지. v3.6부터의 벤치마크는 언어 지시 변경분을 포함하므로 v3.5 수치와 비교 시 명시할 것.

**이 지표의 위상과 한계** (보고서 방어용 — 정직하게 명시할 것):

- 절대치(92%)는 질문 구성에 의존하므로 **고정 벤치마크(같은 세션·같은 질문지·같은 채점자)
  안에서만 절대 비교가 유효**하다 — 프롬프트 버전 간, 압축 모델 간 비교가 그 경우.
  타 연구 수치와의 직접 비교는 불가.
- 질문 생성·답변·채점이 모두 같은 모델 계열(sonnet) — 순환성 위험. 이진 판정·무맥락
  대조(0/130)로 완화했지만 제거는 아니다. 채점 표본의 사람 검수가 보강 후보.
  - **채점자 붕괴 실측 1건과 결정론 가드(2026-08-08)**: OOR 실측 중 채점자가 "모름"↔"모름"
    8건을 전원 정답 처리하는 붕괴가 1회 발생(무맥락 8/8이라는 불가능 수치로 발각 — 무맥락
    대조의 부수 효용). 대응: `"모름" 답변은 채점 판정과 무관하게 오답` 결정론 가드를
    qa-preservation.mjs·compact-vs-builtin.mjs·oor-terms.mjs 전부에 이식. **기존 저장 판정
    2,378건 전수 감사에서 위반 0건** — 본 문서의 발표 수치는 전부 영향 없음 확인.
  - **채점 표본 사람 검수 완료(2026-08-08)**: 층화·블라인드 41건(추출기
    `human-audit-sample.mjs`, AI 판정은 키 파일에 분리) 사람 재판정 결과 —
    **일치율 90% (37/41), Cohen κ=0.61**(표본이 정답 판정 위주로 치우쳐 우연 일치 pe=0.75가
    높은 조건에서의 substantial 경계). **불일치 4건이 2:2 대칭** — 채점자가 보존율을
    부풀리는 방향성 없음. 발표 수치 영향: v3.5의 유일한 비기권 오답 판정을 사람은 정답으로
    봄(92%는 오히려 과소 방향), 내장 비교는 +1/−1 상쇄로 순영향 0 — 90% vs 72% 격차 서사
    유지. 한계: 검수자 1인(팀 내부), 표본 41건.
- 질문은 트랜스크립트에서 생성되므로 "눈에 띄는 사실" 쪽으로 치우칠 수 있다 — 압축도
  같은 사실을 남기기 쉬워 보존율이 과대평가될 수 있음(꼬리 사실은 §2 유형별 오답 분석으로 보완).
- 사실 인출을 재는 것이지 "패키지로 작업을 이어갈 수 있는가"(태스크 성공률)의 대체가
  아니다 — 후자는 §4.3에서 비용상 제외한 SWE-bench류의 영역.

### 목적 프로파일 A/B (2026-08-09 실측 — 프롬프트 v3.7→v3.8.1)

UI 목적 셀렉트(continue/bugfix/handoff)가 프롬프트에 넣는 수신자 프로파일 블록의 효과 검증.
v3.7에서 한국어 한 줄 힌트를 영어 블록으로 승격했고(언어 오염 경로 제거·신호 강도),
v3.8에서 블록 3종을 §2의 실측 손실 모드에 1:1 대응하는 규칙으로 재작성했다.

**설계** — 통제가 핵심: 로컬 한국어 세션 4개(트랜스크립트 49k~69k자)에 대해 같은 구간
(턴 수 고정 — 로컬 로그가 실험 중에도 자라므로 컷 비율이 아니라 절대 턴 수로 재현),
같은 질문지(10문항/세션, 목적 4조건이 질문 생성·무맥락 답변 캐시 공유), 같은 채점자(sonnet),
같은 대조군(none = 목적 미지정, v3.6과 프롬프트 문자열 동일)으로 4조건×4세션 = 160문항.
스크립트: `batch-compact.mjs --purposes` · `qa-preservation.mjs`(project|purpose 키),
원자료: `batch-results/purpose-v37/`·`purpose-v38/`. 압축 비용 $7.2(20회), 채점은
세션·조건당 ~$0.5.

**결과** (같은 질문지 40문항/조건):

| 목적 | v3.7 (일반론 블록) | v3.8 (실측 기반 재작성) | v3.8.1 (handoff 가드) |
|---|---|---|---|
| none (대조군) | 32/40 (80%) | 32/40 (80%) | — |
| continue | 31/40 (78%) | **34/40 (85%)** | — |
| bugfix | 32/40 (80%) | 33/40 (83%) | — |
| handoff | 32/40 (80%) | 30/40 (75%) | 32/40 (80%) |

핵심 발견:

- **v3.8 continue가 +5%p(대조군 대비)·+7%p(v3.7 대비), 4세션 중 3곳 상승·악화 0** —
  상태 정확성·열거 붕괴 금지·작업 파라미터 수치 규칙이 의도한 방향. bugfix도 +3%p.
  목적 예산 가중이 사실 삭제로 번지지 않았다(v3.7 회귀 검사의 통과 기준).
- **v3.8 handoff는 75%로 유일하게 대조군 미달** — 고유 오답 3건이 열거 탈락(GLOSSARY
  미정의 용어 4개), 에러 식별자 탈락(ImportError 모듈명), 제약 희석(채점 경계 사례 1 포함).
  서사·결정 확장이 고정 출력 예산 안에서 식별자·열거를 밀어내는 메커니즘 —
  preamble("사실은 목적과 무관하게 생존")이 블록 본문의 확장 지시를 못 이긴 실측.
  대응(v3.8.1): handoff 블록에 "Narrative never eats facts" 가드 1줄 추가 → 80% 회복
  (오답 구성도 전 조건 공통 꼬리 사실 위주로 정상화).
- **전 조건 공통 오답 5문항은 전부 꼬리 사실**(커밋 해시+제목, 조사에서 나온 도구명,
  문서 속 계산식, 신뢰도 기준값) — 목적 블록과 무관한 기존 손실 모드("정직한 누락",
  환각 0)가 여기서도 유지된다.
- **key-fact recall은 목적 무관 불변**: 수정 파일 full 76/76(100%)·llm 99%, 명령 head
  full 176/176(100%) — 목적 블록이 사실층 병합 보증을 건드리지 않음을 결정론 채점으로 확인.

한계(정직 보고): ① 표본이 4세션×10문항이라 조건 간 1~3문항 차이는 노이즈 경계 —
방향성(3/4세션 일관 상승)과 오답 메커니즘 분석으로 보강했지만 확정적 수치는 아니다.
② v3.8.1 가드는 같은 질문지 재측정으로 검증했으므로 **질문셋 과적합 위험**이 있다 —
가드 문안은 점수가 아니라 오답 메커니즘(열거·식별자·제약)에서 도출했음을 근거로 삼고,
새 질문셋 재검이 보강 후보. ③ QA 보존율은 handoff의 목표 품질(사람 가독성·서사 완결성)을
재지 못한다 — handoff의 이득 측정에는 PREVis류 사람 평가가 필요(§1). ④ 로컬 세션이라
재현 불가·비공개(보고서엔 "로컬 트랙" 명시).

## 3. 컨텍스트 압축 연구의 지표·보고 관행 (비교 문맥용)

### 3.1 효율성(Efficiency) 지표 — 우리가 쓸 수 있는 것만

컨텍스트 압축 논문들이 품질 축과 함께 보고하는 효율성 축 중 BranchPort에 해당하는 것:

| 지표                  | 정의                                                          | BranchPort 적용                                 | 출처                                                                                                                     |
| --------------------- | ------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| **토큰 절감률** | 압축 후/전 토큰 수 비율 (예: 54% 절감, 4× 압축)              | ✅ LLM 요약 기능의 입력 토큰 절감으로 보고 가능 | [ACON](https://arxiv.org/pdf/2510.00615) · [OPENDEV](https://arxiv.org/pdf/2603.05344)                                    |
| **비용 절감**   | 질문(태스크)당 토큰 비용(USD)                                 | ✅ 요약 기능 도입 시 산출 가능                  | [SelfCompact](https://arxiv.org/abs/2606.23525) (30\~70% 절감 보고)                                                       |
| **압축 시간**   | 압축(요약 생성) 자체에 걸리는 시간 — 순절감 계산의 차감 항목 | △ 요약 생성 시간만 해당                        | [Prompt Compression in the Wild](https://arxiv.org/html/2604.02985) · [경량 압축(엣지)](https://arxiv.org/pdf/2606.20571) |

그 외 효율성 지표(GPU 메모리·KV 캐시·처리량·TTFT/TBT·percentile 지연시간)는 **모델 서빙 지표라 로컬 시각화 도구인 우리와 무관** — §4.3의 제외 목록 참고. 용어 주의: 일부 2차 자료가 쓰는 "FTIT"는 비표준 표기이며 표준 용어는 **TTFT**.

### 3.2 성능(Effectiveness) 지표 — 압축 연구 관행

- **F1 / Exact Match(EM)**: RAG·QA형 평가의 표준. 압축 전후 점수 비교로 "품질 보존율"을 산출 — §2의 key-fact recall이 우리 버전에 해당.
- **BLEU / ROUGE-L**: 압축 표현에서 원문을 복원(reconstruction)하는 품질 측정에 사용 ([PCC](https://aclanthology.org/2025.acl-long.1394.pdf)). 우리의 구조적 압축은 무손실이라 재구성 오차 0으로 갈음(§1).
- **작업별 점수**: 수학·에이전트 탐색 등 다운스트림 태스크 점수 변화로 압축 품질을 간접 측정 (SelfCompact 방식).

### 3.3 대표 연구 수치 (원논문 검증 완료)

- **SelfCompact** ([arXiv 2606.23525](https://arxiv.org/abs/2606.23525), 2026-06 · [코드](https://github.com/tianjianl/selfcompact)): 모델 스스로 압축 시점을 판단(발동/억제 루브릭 + 요약 툴). 무요약 대비 수학 최대 **+18.1점**, 에이전트 탐색 **+5\~9점**, 질문당 비용 **30\~70% 절감**. 50\~100k 토큰 궤적을 1\~3k로 요약(20\~80× 축소). → **"언제 압축하느냐"가 품질을 좌우한다는 근거**로 인용 가치.
- **PCC**(Pretraining Context Compressor, [ACL 2025](https://aclanthology.org/2025.acl-long.1394.pdf), Microsoft): 임베딩 기반(소프트 프롬프트) 압축. 4× 압축 시 평균 F1 **63.62** vs 원본 컨텍스트 **66.29** (≈96% 유지, Llama-3-8B 기준), 4×/16×를 권장 압축률로 제시. 극단 압축(256×)은 정보 손실 급증 — LLMLingua의 경향과 일치.
- **LLMLingua 계열**: 압축률(2x\~20x) + 다운스트림 성능 유지율 + 지연시간을 함께 보고. 20x 이하 추출식 압축은 95%+ 정확도 유지, 극단 압축(480x)은 62\~73%로 급락. ([LLMLingua-2](https://arxiv.org/pdf/2403.12968))
- **ACON**(Microsoft, 2025): 에이전트 컨텍스트 압축을 peak 토큰 26\~54% 감소 + 태스크 성공률 유지/향상으로 평가. ([논문](https://arxiv.org/pdf/2510.00615))
- **대화 메모리 벤치마크**: LoCoMo, LongMemEval, MemBench — QA 정확도 기반. 우리가 그대로 쓰긴 어렵지만 "압축률↑ ↔ 수치·시간 정보 충실도↓" 트레이드오프 보고 관행 참고.
- **needle-in-haystack 변형**: "요약 과정에서 소실된 정보가 나중에 필요해지는" 시나리오 평가 ([ARC](https://arxiv.org/html/2607.25066)).

### 3.4 CLI 코딩 에이전트 특화 지표 — 우리와 가장 가까운 도메인

- **OPENDEV의 Adaptive Context Compaction(ACC)** ([arXiv 2603.05344](https://arxiv.org/pdf/2603.05344) §2.3.6): 툴 관측값이 컨텍스트의 70\~80%를 차지한다는 전제에서, 5단계 점진 압축(임계값 70/80/85/90/99%)으로 관측값을 active→faded→archived로 전이. 오래된 툴 결과를 ~15토큰짜리 참조 포인터로 치환. **정량 결과: 관측값 peak 컨텍스트 소비 약 54% 절감, 통상 30턴 세션에서 긴급 압축(emergency compaction)을 대부분 제거** (원문 확인 완료).
  - **BranchPort 서사와의 접점**: OPENDEV는 전체 히스토리를 아카이브 파일로 남기고 요약에 경로를 주입해 "사실상 무손실(effectively non-lossy)"을 주장 — 우리의 "턴을 펼치면 원본 레코드 100% 복원"과 동일한 설계 철학. related work 인용 1순위 후보.
  - **긴급 압축 빈도**(N턴 세션당 긴급 요약 횟수)는 압축 *전략*의 평가 지표로 유용 — Claude Code `/compact` 발생 시점을 시각화하는 우리에게는 "측정 대상"이지 "우리 도구의 성능 지표"는 아님.
- **OpenHands 컨텍스트 condenser 실측** ([공식 블로그, 2025-04](https://www.openhands.dev/blog/openhands-context-condensensation-for-more-efficient-ai-agents)): SWE-bench Verified 부분집합에서 condenser 적용 시 해결률 **54% vs 베이스라인 53%**(성능 유지)에 **API 비용 절반 이하**. "성능 손실 없는 비용 50% 절감"의 대표 실측 사례 — 단, 벤더 자체 보고로 명시하고 인용.
- **압축 하의 에이전트 태스크 지표**: 압축 연구는 "코드 생성 정확도·파일 수정 정확도" 같은 별도 지표 대신 **다운스트림 태스크 성공률**(SWE-bench 해결률, DiscoveryBench 등)로 압축 영향을 측정하는 것이 관행. [Evaluating Memory Condensation Strategies for Coding Agents](https://arxiv.org/pdf/2605.18854)는 OpenHands 기반으로 태스크 성공률 + 툴 호출 성공 + 토큰 비용 + **압축 발생 횟수(compression frequency)** 를 지표 세트로 사용 — "세션당 압축 횟수"의 학술 출처.
- **툴 호출 신뢰도 경계** ([Control Under Compression](https://arxiv.org/html/2608.01056)): 툴 사용 에이전트의 컨텍스트를 예산별로 압축하면 성공률이 3구간으로 나뉨 — **예산 75%까지는 풀 컨텍스트 대비 1.4%p 이내 유지, 50\~35% 구간에서 방법 간 격차가 벌어지며 급락**. "어디까지 압축해도 되는가"의 정량 근거.
- **컨텍스트 썩음(context rot)** ([Chroma 기술 보고서](https://www.trychroma.com/research/context-rot)): 입력이 길어질수록 단순 검색 과제조차 성능이 비선형 하락 — 18개 SOTA 모델에서 확인. **압축이 왜 필요한가의 동기 근거**로 인용 (압축 품질 지표 자체는 아님). 일부 2차 자료가 말하는 "Context Rot **Index**"라는 표준화된 지수는 존재하지 않음 — 현상 명칭일 뿐이니 지표로 인용하지 말 것.
- **유효 컨텍스트 윈도우**(모델 윈도우 − 예약 출력 토큰): 실무 개념으로는 유용하나 학술 출처가 약함 — 보고서에 쓴다면 정의만 차용하고 수치 주장은 하지 않기.

### 3.5 트레이드오프 곡선 보고 관행

단일 수치 대신 **곡선/frontier로 보고**하는 흐름이 최근 표준으로 자리잡는 중:

- **정확도 vs 압축률 곡선**: [ACON](https://arxiv.org/pdf/2510.00615)은 accuracy–peak tokens 평면에서 방법들을 비교. [End-to-End Context Compression at Scale](https://arxiv.org/html/2606.09659)은 압축 시간–정확도 평면의 **Pareto frontier**로 우위를 주장.
- **비용–성능 frontier**: [The Efficiency Frontier](https://arxiv.org/html/2605.23071v1)는 컨텍스트 관리 전략 전반을 비용–성능 최적화 문제로 통합 정식화.
- **신뢰도 frontier**: 위 Control Under Compression의 3구간 경계 — 압축을 "토큰 절감 문제"가 아니라 "런타임 운영 결정"으로 재정의.
- **BranchPort 시사점**: 우리가 요약 기능을 평가할 때도 단일 압축률이 아니라 **2\~3개 압축 수준에서 key-fact recall을 측정해 곡선으로 제시**하면 학계 관행과 맞는 보고가 됨 (§4.1 우선순위 5).

## 4. 권고안 — 결과보고서 지표 구성

**보고 틀**: 결과보고서의 주장을 둘로 분리하고, 각 주장을 "압축률 + 정보 보존율" 2축 세트로 보고 (학계 관행 차용).

- **주장 A — "흩어진 로그를 한 화면으로"** (접기·시각화, 이미 구현): 가독성·탐색 효율.
- **주장 B — "compact로 컨텍스트 비용 절약"** (LLM 요약, 기능 도입 가정): 트리에서 선택한 구간을 요약해 새 세션에 이어붙일 압축 컨텍스트를 생성 → 토큰·비용 절감을 품질 보존과 함께 증명.

> 주장 A의 압축률(레코드→턴 94%)을 주장 B의 근거로 쓰면 안 됨 — *화면* 압축이지 *컨텍스트 토큰* 압축이 아니라서 범주 오류. 두 주장의 수치를 섞지 말 것.

### 4.1 지표 우선순위

| 우선순위 | 지표                                                        | 주장 | 측정 방법 · 근거                                                                                                                                     | 측정 비용           |
| -------- | ----------------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| 1        | 구조적 압축률 (레코드→턴, 실측 ≈94%)                      | A    | 즉시 계산 가능 (실측 완료)                                                                                                                            | 즉시                |
| 2        | 재구성 완전성 (무손실 — 오차 0)                            | A    | "턴을 펼치면 원본 100% 복원" — OPENDEV의 'effectively non-lossy'와 동일 철학(§3.4)                                                                  | 즉시                |
| 3        | 토큰 절감률 + 세션당 비용(USD)                              | B    | 원본 구간 vs 압축본 토큰 수(토크나이저 자동 산출) × API 단가. SelfCompact(30\~70%)·OPENDEV(54%)와 동일 형식 — 심사위원에게 가장 직관적             | 즉시                |
| 4        | Key-fact recall (수정 파일·명령 보존율)                    | B    | **절감률 주장의 필수 짝** — 단독 절감률은 "품질을 버린 것 아니냐"에 무방비. 툴콜 레코드에서 정답 기계 추출 → 자동 채점. 우리 데이터 최대 강점 | 낮음                |
| 4b       | QA 보존율 (§2 파일럿)                                       | B    | recall의 짝 — 서술형 사실(결정·에러 경위·상태)의 보존을 잼. 파일럿 실측 90% (10문항·1세션·$0.36). 무맥락 대조로 찍기 보정                       | 낮음 (LLM 4회/세션) |
| 5        | 트레이드오프 곡선 (압축 수준 2\~3개 × recall)              | B    | 단일 수치보다 설득력 높음. "어디까지 압축해도 안전한가" — Control Under Compression(§3.4)과 비교 가능                                               | 낮음                |
| 6        | SummaC 사실적 일관성                                        | B    | recall과 다른 각도(환각 없음)의 품질 보강                                                                                                             | 낮음\~중간          |
| 7        | 과제 완료 시간 A/B (원시 로그 vs BranchPort, Githru식)      | A    | *시간* 절약 어필 — 비용 절약(B)과 별도 주장으로 분리                                                                                               | 중간 (팀 내부 실험) |
| 8        | 화면/스크롤 감소율, 질의 보존율                             | A    | 원시 JSONL 뷰 vs 턴 뷰 비교                                                                                                                           | 낮음                |
| 보조     | 압축 소요 시간 · 툴콜 주체 오귀속 비율 · G-Eval 이진 판정 | B    | 압축 소요 시간은**순절감 = 절감 토큰 비용 − 요약 생성 비용** 계산에 필요 — 공정 보고용                                                        | 낮음\~중간          |

### 4.2 주장 B의 설계 원칙

- **baseline 명시**: "무압축 대비"는 수치가 크게 나오지만 공정한 비교는 **Claude Code 내장 `/compact` 대비**. `/compact`를 정량 평가한 벤치마크는 없으므로(결론 4), 같은 세션에 대해 `/compact` 결과물과 BranchPort compact 결과물의 (토큰 수, key-fact recall)을 나란히 재면 **그 자체가 최초 실측 — 대회 차별점**. → **실측 완료(2026-08-08)**: 11세션 맞대결에서 QA 보존율 90% vs 72%, 파일 recall 100% vs 67% (§2 "내장 `/compact` 대비 비교").
- **순절감으로 보고**: 요약 생성에도 토큰이 들므로, 절감 토큰 비용에서 요약 생성 비용을 뺀 값을 제시해야 공정.
- **최종 구성**: 토큰 절감률 + 세션당 비용 + key-fact recall을 2축 세트로, 가능하면 압축 수준별 곡선(우선순위 5)으로.

### 4.3 조사했으나 제외한 지표

| 지표                                                   | 제외 사유                                                                                                                     |
| ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| GPU 메모리·KV 캐시·처리량·TTFT/TBT·percentile 지연 | 모델 서빙 지표 — 로컬 시각화 도구가 주장하면 측정 불가능한 수치를 흉내 내는 셈                                               |
| Perplexity, BLEU/ROUGE(재구성)                         | 소프트 프롬프트 압축 계열 전용 / 우리 구조적 압축은 무손실이라 불필요                                                         |
| SWE-bench류 태스크 성공률                              | 압축 품질의 최종 증명이긴 하나 재현 비용이 대회 일정 대비 과대 — key-fact recall + 소규모 A/B로 갈음                         |
| 긴급 압축 빈도·세션당 압축 횟수                       | 압축*전략*의 지표. 우리에겐 시각화 **대상**이지 성능 지표가 아님. "`/compact` 발동 감소" 통제 실험은 여력 있을 때만 |
| Context rot, 유효 컨텍스트 윈도우                      | 지표가 아니라 동기 서사·실무 개념 — 도입부 인용에만 사용, 수치 주장 금지                                                    |
| SummEval 4차원 체크리스트                              | 채점 프레임으로는 유효하나 우선순위 지표들과 중복 — 필요 시 §2 참고                                                         |

**주의사항**:

- 벤더 자체 보고 수치(예: AgentPrism "디버깅 시간 80% 감소", OpenHands condenser)를 인용할 땐 "자체 보고 사례"로 명시.
- 일부 원문 PDF(Githru 원문, Aggregate-Driven Trace Visualizations)는 에이전트가 텍스트 추출에 실패해 초록/2차 출처까지만 확인 — 세부 수치 인용 전 직접 열람 필요.
- AI 검색 결과를 조사에 반영할 땐 반드시 원논문으로 역추적할 것. 이번 2차 조사에서 블로그·위키 인용으로 제시된 수치들(SelfCompact 30\~70%, PCC F1 63.62/66.29, OPENDEV 54%)은 **전부 원논문에서 실재 확인**했으나, "FTIT" 같은 비표준 용어가 섞여 있었음(표준: TTFT).

## 5. 평가용 데이터셋 조사 (2026-08-07)

QA 보존율·한국어 트랙 표본 확보를 위해 조사. 결론: **코딩 세션에 대한 기성 QA 데이터셋은 없다** —
트랜스크립트에서 QA를 자체 생성하는 방식(§2)이 정공법이고, 기성 벤치마크는 방법론만 차용한다.

### 5.1 코딩 세션 데이터셋 (압축 대상)

| 데이터셋 | 규모 | 라이선스 | 한국어 | 비고 |
|---|---|---|---|---|
| trace-commons · Fable-5 (기도입) | 106세션 | CC-BY-4.0 / AGPL | ✕ 전부 영어 | [실험 문서](./2026-08-06-압축-데이터셋-실험-결과.md) 참고 |
| [SWE-chat](https://huggingface.co/datasets/SALT-NLP/SWE-chat) (SALT-NLP) | 5,851세션 · 2.69M 대화 | ODC-BY (gated — HF 로그인+동의 필요) | **✅ 실측 확인: 한국어 프롬프트 89건 · 세션 31개** | 전부 Claude Code 세션. API콜 20회 이상의 실질 작업 세션 13개(최대 376콜·출력 19만 토큰). 대표 2세션 다운로드→BranchPort 파서 검증 완료(916줄·122줄, 파싱 에러 0). 세션 목록: `오픈소스/probe-test/datasets/swechat-korean/sessions.json` |
| 로컬 실사용 세션 | 한국어 약 25개 (전체 188개 중 한글 15%+ 37개, Temp의 `claude -p` 인공 세션 제외) | — | ✅ | 한국어 트랙(언어 보존·한글 절약율) 표본으로 충분. 재현 불가·비공개라 보고서엔 "로컬 트랙" 명시 |

### 5.2 QA 벤치마크 (방법론 차용용 — 직접 사용 부적합)

| 벤치마크 | 내용 | 차용할 것 |
|---|---|---|
| [LongMemEval](https://github.com/xiaowu0162/longmemeval) (ICLR 2025, [HF](https://huggingface.co/datasets/xiaowu0162/longmemeval-cleaned)) | 긴 어시스턴트 대화(평균 115k 토큰) QA 500문항 | **질문 5유형**(정보 추출/멀티세션 추론/지식 갱신/시간 추론/**abstention**) — 특히 "모름" 판정 평가 |
| [LoCoMo](https://snap-research.github.io/locomo/) | 장기 대화(300턴) QA | 질문 유형 분류(단일홉/멀티홉/시간순/적대적) — §2 질문 생성의 유형 분산에 이미 반영 |
| [KorQuAD](https://korquad.github.io/) | 한국어 위키 문서 독해 100k+ 쌍 | 도메인 불일치(문서 MRC ≠ 세션). 한국어 QA 채점기의 sanity check 정도 |

부적합 사유 공통: 전부 일반 대화·문서 도메인이라 코딩 세션(도구 호출·diff·에러)의 압축 품질을 대변하지 못함.

### 5.3 일반(비코딩) 대화 데이터셋 — 언어 다양성 트랙 후보

| 데이터셋 | 규모·언어 | 라이선스 | 판단 |
|---|---|---|---|
| [WildChat-1M](https://huggingface.co/datasets/allenai/WildChat-1M) | 실사용자↔ChatGPT 100만 대화, 68개 언어(영 53%·중 13%·러 12%, 한국어 소수 포함) | ODC-BY | 실사용 대화라는 점은 매력적이나 **Claude Code jsonl이 아님** — 쓰려면 변환 어댑터 필요 |
| [LMSYS-Chat-1M](https://huggingface.co/datasets/lmsys/lmsys-chat-1m) | 25개 모델 100만 대화, 언어 태그 포함 | 자체 라이선스(연구용) | 동일 — 형식 변환 필요 + 재배포 제약 |
| AI Hub 한국어 대화류 | 한국어 일상·상담 대화 | 약관 기반(재배포 제한) | 코딩 무관 + 라이선스 부담 — 보류 |

**판단**: compact 프롬프트는 코딩 세션(사실층·파일·명령·에러) 전제로 설계·검증됐으므로 일반 대화
데이터셋은 **주력 평가에 부적합**. 굳이 쓴다면 "언어 보존 규칙이 비영어 대화에서도 작동하는가"의
보조 실험 정도인데, 이는 로컬 한국어 세션 25개(§5.1)로 이미 커버 가능 — **변환 어댑터 비용을 지불할
이유가 현재로선 없다**. SWE-chat 한국어 필터링이 실측으로 확인되어(§5.1: 31세션·실질 13개)
"공개+한국어+코딩 세션" 표본은 그쪽으로 확보됐다.

## 다음 단계

1. 구조적 압축률·재구성 완전성(우선순위 1\~2)은 코드에서 바로 산출 가능 — 통계 엔드포인트/화면에 노출 검토.
2. ~~compact 기능 구현 시 토큰 절감률·세션당 비용(우선순위 3)을 함께 출력하도록 설계~~ **완료** —
   103세션 실측은 [실험 문서](./2026-08-06-압축-데이터셋-실험-결과.md). ~~`/compact` 대비 비교(§4.2)~~
   **완료(2026-08-08)** — 실제 프로덕션 산출물 11세션 맞대결, QA 90% vs 72%·파일 recall 100% vs 67%(§2).
3. ~~Key-fact recall(우선순위 4) 자동 채점 스크립트~~ **완료(2026-08-08)** — `keyfact-recall.mjs`,
   7개 배치 소급 채점: 수정 파일 full recall **전 트랙 100%**(사실층 병합 보증 실측), llm층 단독은
   32\~78%(§2 Key-fact recall). ~~후속 후보: 사실층 명령 20개 캡 상향, 내장 `/compact` 산출물 채점(§4.2)~~
   **둘 다 완료(2026-08-08)** — 캡 60 상향(명령 recall 97%, §2 컷 곡선 ⑧) · 내장 산출물 채점(§2 내장 비교).
4. ~~QA 보존율 확장(우선순위 4b)~~ **완료(2026-08-07)** — 13세션·130문항 배치: 보존율 86%,
   무맥락 0%(필터 유효), 스크립트 `qa-preservation.mjs` 이관 완료(§2 배치 실측).
   ~~잔여: LoCoMo식 질문 유형별 집계~~ **완료** — 5유형 분류·집계(§2 표). ~~수치 보존 강화
   프롬프트 규칙~~ **완료** — v3.5 numeric preservation 규칙으로 86%→92% 회귀 확인(§2 회귀 측정 표).
   ~~보강 후보: 채점 표본 사람 검수~~ **완료(2026-08-08)** — 블라인드 41건 일치 90%·κ=0.61(§2 "위상과 한계").
5. ~~SWE-chat 한국어 필터링~~ **완료(2026-08-07)** — 한국어 세션 31개(실질 13개) 확인·파싱 검증(§5.1).
   후속: 실질 13세션 트랜스크립트 일괄 다운로드 → 한국어 트랙 배치(compact+QA 보존율)에 로컬 25세션과 병행 투입.
   주의: 언어 판정은 프롬프트 단위라 1\~2건짜리 세션은 영어 위주 세션일 수 있음 — 트랙 투입 전 한국어 비중 재검.
6. 요약 기능 구현이 진행되면 SummaC 파이프라인 시범 적용.
7. 제출 전 팀 내부 A/B 실험(과제 완료 시간) 1회 실시.
8. ~~목적 프로파일 실측~~ **완료(2026-08-09)** — v3.7→v3.8.1 A/B, 4세션×4조건×10문항
   (§2 "목적 프로파일 A/B"). 잔여: 새 질문셋으로 v3.8.1 handoff 가드 재검(과적합 방어).

## 출처 전체 목록

- 요약 품질: [BERTScore](https://arxiv.org/pdf/1904.09675) · [SummaC](https://github.com/tingofurro/summac) · [QAFactEval](https://arxiv.org/pdf/2112.08542) · [QuestEval](https://github.com/ThomasScialom/QuestEval) · [G-Eval](https://arxiv.org/abs/2303.16634) · [SummEval](https://github.com/Yale-LILY/SummEval) · [LLM Judge 신뢰성 2026](https://arxiv.org/pdf/2603.05399)
- 컨텍스트 압축: [LLMLingua-2](https://arxiv.org/pdf/2403.12968) · [LongLLMLingua](https://arxiv.org/pdf/2310.06839) · [ACON](https://arxiv.org/pdf/2510.00615) · [정보 보존](https://arxiv.org/html/2503.19114) · [ARC](https://arxiv.org/html/2607.25066) · [Prompt Compression in the Wild](https://arxiv.org/html/2604.02985) · [SelfCompact](https://arxiv.org/abs/2606.23525) ([코드](https://github.com/tianjianl/selfcompact)) · [PCC (ACL 2025)](https://aclanthology.org/2025.acl-long.1394.pdf) · [경량 압축(엣지 QA)](https://arxiv.org/pdf/2606.20571) · [compaction 커뮤니티 정리](https://gist.github.com/badlogic/cd2ef65b0697c4dbe2d13fbecb0a0a5f)
- CLI 에이전트·장문 컨텍스트: [OPENDEV — Building Effective AI Coding Agents for the Terminal](https://arxiv.org/pdf/2603.05344) · [Chroma Context Rot](https://www.trychroma.com/research/context-rot) · [OpenHands Condenser 블로그](https://www.openhands.dev/blog/openhands-context-condensensation-for-more-efficient-ai-agents) · [코딩 에이전트 메모리 압축 평가](https://arxiv.org/pdf/2605.18854) · [Control Under Compression](https://arxiv.org/html/2608.01056) · [LLM 추론 지표 핸드북 (BentoML)](https://bentoml.com/llm/inference-optimization/llm-inference-metrics)
- 트레이드오프·frontier: [ACON](https://arxiv.org/pdf/2510.00615) · [End-to-End Context Compression at Scale](https://arxiv.org/html/2606.09659) · [The Efficiency Frontier](https://arxiv.org/html/2605.23071v1)
- 대화 요약: [DialSummEval](https://aclanthology.org/2022.naacl-main.418/) · [대화 요약 오류 분석](https://arxiv.org/pdf/2305.16548) · [QAGS](https://aclanthology.org/2020.acl-main.450/)
- 구조 집약·시각화: [Graph Summarization Survey](https://gemslab.github.io/papers/liu-2018-graph-summarization.pdf) · [CGS](https://arxiv.org/html/2607.10969v1) · [Autofolding/TASSAL](https://arxiv.org/abs/1403.4503) · [Githru](https://arxiv.org/abs/2009.03115) · [PREVis](https://arxiv.org/abs/2407.14908) · [Event Sequence 서베이](https://arxiv.org/pdf/2006.14291)
- 메모리 벤치마크: [LoCoMo/LongMemEval 정리](https://deepwiki.com/agentscope-ai/ReMe/15.5-longmemeval-and-locomo-benchmarks) · [mem0 벤치마크 2026](https://mem0.ai/blog/ai-memory-benchmarks-in-2026)
- 데이터셋 (§5, 2026-08-07 추가): [SWE-chat](https://huggingface.co/datasets/SALT-NLP/SWE-chat) ([논문](https://arxiv.org/pdf/2604.20779)) · [LongMemEval](https://github.com/xiaowu0162/longmemeval) ([HF](https://huggingface.co/datasets/xiaowu0162/longmemeval-cleaned)) · [LoCoMo](https://snap-research.github.io/locomo/) · [KorQuAD](https://korquad.github.io/) · [WildChat-1M](https://huggingface.co/datasets/allenai/WildChat-1M) · [LMSYS-Chat-1M](https://huggingface.co/datasets/lmsys/lmsys-chat-1m) · [HF Agent Traces 허브](https://huggingface.co/docs/hub/agent-traces)
