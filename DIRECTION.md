# DIRECTION.md

팀 내부용 방향성 문서입니다. **왜 만드는지, 어디로 가는지, 그리고 무엇을 하지 않을지**를 적습니다.

> 역할 구분
> - [README.md](./README.md) — **바깥 사람용**. 이게 뭐 하는 도구고 어떻게 실행하는가
> - **이 문서** — **팀 내부용**. 왜 만드는가, 어디로 가는가, 무엇을 안 하는가
> - [CLAUDE.md](./CLAUDE.md) — 작업 규칙 · [GLOSSARY.md](./GLOSSARY.md) — 용어

---

## 왜 만드는가

Claude Code는 대화를 `~/.claude/projects/**/*.jsonl`에 쌓아두지만, 이건 사람이 읽으라고 만든 형식이 아닙니다.

- 세션이 여러 파일로 흩어집니다 (이어받기 하면 새 파일이 생기고 앞부분이 중복됩니다)
- 서브에이전트 기록은 아예 다른 폴더에 따로 저장됩니다
- 도구 호출·훅 출력·시스템 알림이 실제 대화와 뒤섞여 있습니다
- 되돌리기를 하면 실제로는 트리가 갈라지는데, 파일은 그냥 평평한 줄 목록입니다

그래서 **"내가 지난주에 이 파일을 왜 이렇게 고쳤더라"를 되짚는 비용이 지나치게 큽니다.**

BranchPort는 이 흩어진 기록을 **한 장의 노선도로 합쳐서**, 작업의 흐름과 갈라진 지점을 눈으로 보게 합니다. 나아가 특정 구간을 골라 **다른 대화가 이어받을 수 있는 인수인계 문서로 압축**하는 것이 최종 목표입니다.

---

## 핵심 원칙

이 세 가지는 기능을 추가할 때마다 되묻는 기준입니다.

**1. 완전 로컬**
서버는 `127.0.0.1`에만 바인딩합니다. 세션 로그에는 코드 원문·파일 경로·때로는 자격증명까지 들어 있습니다. 이 데이터가 기기 밖으로 나가는 경로를 만들지 않습니다.

**2. 런타임 의존성 0개**
Node 내장 모듈과 HTML 파일 하나로 동작합니다. 진입 장벽을 낮게 유지하고, 공급망 위험을 원천 차단하며, 몇 년 뒤에도 그냥 돌아가게 하기 위해서입니다.

**3. 읽기 전용**
원본 세션 로그를 **절대 수정하거나 삭제하지 않습니다.** BranchPort는 관찰 도구이지 편집기가 아닙니다. 이 원칙 덕분에 "잘못 눌러서 대화 기록이 날아갈까" 걱정 없이 쓸 수 있습니다.

---

## Non-Goals — 하지 않을 것

에이전트는 시키지 않은 것까지 "도움이 될 것 같아서" 만드는 경향이 있습니다. **아래 항목이 필요해 보이면, 만들기 전에 먼저 이슈로 논의합니다.**

- **클라우드 동기화 · 서버 배포 · 팀 공유 기능을 만들지 않습니다.** 원칙 1과 정면으로 충돌합니다.
- **세션 로그를 편집·삭제·병합하는 기능을 만들지 않습니다.** 원칙 3 위반입니다.
- **프레임워크(React/Vue 등)나 번들러를 도입하지 않습니다.** 원칙 2 위반입니다. `public/index.html`은 단일 파일로 유지합니다.
- **인증 · 멀티유저 · 권한 관리를 만들지 않습니다.** 1인 로컬 도구입니다.
- **Claude Code 외 다른 도구의 로그 포맷을 범용 지원하지 않습니다.** 지금은 한 포맷을 제대로 다루는 것이 우선입니다.
- **데이터베이스를 도입하지 않습니다.** 매 요청마다 파일을 다시 읽는 현재 방식이 단순하고 항상 최신입니다. 성능 문제가 실측으로 확인되기 전까지는 캐시도 넣지 않습니다.

---

## 지금 우선순위

> ⚠️ 이 섹션은 초안입니다. 팀 논의 후 채워 넣으세요.

**알려진 문제**
- 위임(`deleg`) 전용 턴이 회색(`chat`)으로 표시됩니다. `phaseOf()`가 `deleg` 카테고리를 색에 반영하지 않아, Agent 호출만 있는 무거운 턴이 "대화"로 보입니다. (`src/turns.ts:9`) — 의도된 설계인지 확인 필요.

**논의가 필요한 것**
- 대규모 세션(수천 노드)에서의 렌더 성능 기준을 정할 것인지
- 압축 패키지의 요약 품질을 어떻게 평가할 것인지

**다음 목표**
- (팀 논의 후 작성)
