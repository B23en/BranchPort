# 의존성 / 빌드 산출물
node_modules/
dist/

# 대화 내용에서 파생된 데이터 — 절대 커밋하지 않는다
# packages: 압축 패키지(대화 원문 일부 포함)
# labels:   LLM이 지은 턴 제목·요약 캐시
# branches: 갈래 대화 기록
# index:    검색 인덱스(턴 제목·요약·식별자)
packages/
labels/
branches/
index/

# OS / 편집기 부산물
.DS_Store
*.swp

# 로그 / 비밀값
*.log
.env
.env.*
